# 테스트 케이스 설계서

본 문서는 comfps 프로젝트의 단위 테스트 전반을 요약하고, 각 테스트의 목적(시나리오), 입력, 기대 결과를 간단히 정리합니다. 모든 테스트는 JUnit 5 기반이며 Maven으로 실행됩니다.

## 공통 사항
- 환경: Java 17, Maven, JUnit Jupiter
- 실행: 루트에서 `mvn test` (VS Code 터미널에서 실행)
- 성공 기준: 모든 모듈(commons/client/server) 테스트 통과, 빌드 성공

---

## 1) comfps-common

### ExpSmootherTest
- 목적: 지수 평활(EMA) 로직의 초기화/갱신/경계/예외 동작 확인
- 주요 케이스
  - testInitialState: 초기화 전 상태와 기본 출력값(0.0) 확인
  - testFirstSample: 첫 샘플 입력 시 내부 상태 초기화 및 그대로 반영
  - testSmoothing: y = y + alpha*(x - y) 수식 검증
  - testLowAlpha/testHighAlpha: 작은/큰 alpha에서 수렴 특성 확인
  - testResetTo/testResetAfterReset: 내부 상태 재설정/초기화 시나리오
  - testWithTimeConstant: 시간상수 기반 팩토리 정상 동작
  - testAlphaBoundaryZero/One: 경계 alpha=0/1 동작
  - testInvalidAlphaNegative/AboveOne: 잘못된 alpha에 대한 예외
  - testInvalidTimeConstantZero/Negative: 잘못된 시간상수에 대한 예외
  - testSequentialSmoothing: 연속 입력에 대한 누적 결과
  - testNoisyDataSmoothing: 노이즈 데이터 평탄화 특성

### CooldownTimerTest
- 목적: 쿨다운 타이머의 시작/진행/완료/에지 동작 검증
- 주요 케이스
  - 초기 상태, start 후 isReady/remaining/progress01, forceReady, 0/음수 duration, setDuration, 짧은 쿨다운, 중복 start, 경합 상황 등

### ProtocolTest
- 목적: 스트림 기반 프레임 I/O와 각 Opcode 메시지의 직렬화/역직렬화 검증
- 주요 케이스
  - 프레임 쓰기/읽기 왕복, CHAT/핑퐁/WELCOME/READY_TOGGLE/SET_SELECTION/MAP_VOTE/PHASE_UPDATE/COUNTDOWN/BYE 전송·파싱
  - put/get (int, long, byte, utf8) 원시 타입 직렬화 검증
  - 빈/대용량 페이로드 처리, 잘못된 프레임 길이 예외 처리, opcode 상수 검증

### MathfTest
- 목적: 수학 유틸리티 함수의 정확성 및 경계 동작
- 주요 케이스
  - clamp/saturate/lerp/unlerp/remap/smoothstep/smootherstep/wrapAngle/approach/nearlyEqual
  - 외삽/경계/동일 범위/음수 범위 등 에지 케이스 포함

### Vec2Test
- 목적: 2D 벡터 연산의 정확성
- 주요 케이스
  - 생성자, set/복사/copy, add/sub/scale, len/len2/normalize, distance, dot/cross, epsilonEquals, mul(불변성) 

### SpawnManagerTest
- 목적: 팀별 랜덤 스폰 시스템의 정확성 및 공정성 검증
- 주요 케이스
  - testVerticalPattern: RED(12시/북쪽) vs BLUE(6시/남쪽) 위치 검증
  - testHorizontalPattern: RED(9시/서쪽) vs BLUE(3시/동쪽) 위치 검증
  - testDiagonalNESWPattern: RED(2시/북동) vs BLUE(8시/남서) 위치 검증
  - testDiagonalNWSEPattern: RED(10시/북서) vs BLUE(4시/남동) 위치 검증
  - testGetSpawnByTeam: getSpawn(Team) 메서드 팀별 위치 반환 검증
  - testRandomPatternSelection: null 전달 시 랜덤 패턴 선택
  - testDefaultConstructorRandomPattern: 기본 생성자 랜덤 패턴 선택
  - testSpawnWithinBounds: 모든 패턴에서 맵 경계 내 위치 보장
  - testSpawnDistanceIsSufficient: 패턴별 최소 거리 검증 (VERTICAL: Y축 60%+, HORIZONTAL: X축 60%+, DIAGONAL: 1200+)
  - testToStringAndDescription: 디버깅 정보 출력 검증
  - testReproducibilityWithSeed: 동일 시드로 동일 위치 재현성 검증


### SnapshotV2Test
- 목적: 스냅샷 v2 바이너리 포맷 입출력/정합성
- 주요 케이스
  - 빌드→파싱 라운드트립, 빈/널 처리, parseToMap 매핑, 버전/카운트/절단(truncated) 예외 처리, 부동 소수 정밀, 바이너리 크기 검증

---

## 2) comfps-client

### CharacterRenderFilterTest
- 목적: 유효하지 않은 캐릭터가 화면에 렌더링되지 않는지 검증
- 주요 케이스
  - testDeadCharacterFiltered: HP 0인 캐릭터 필터링
  - testInvalidCharacterIdFiltered: 잘못된 characterId 필터링 (-1, 999)
  - testOutOfBoundsFiltered: 맵 밖 캐릭터 필터링
  - testValidCharactersOnly: 유효한 캐릭터만 렌더링
  - testLowHealthCharacterRendered: HP 1인 캐릭터는 렌더링됨
  - testBoundaryCharacterRendered: 맵 경계선 위 캐릭터 렌더링

### NetClientTest
- 목적: 네트워크 클라이언트의 리스너 연결 경로 준비 상태 확인(비 I/O)
- 주요 케이스
  - 클라이언트 생성, onChat/onWelcome/onPing/onPong/onDisconnected/onPhaseUpdate/onSnapshotV2 콜백 경로 확인
  - 단일 리스너에서 다중 콜백 메서드 구성 확인

### CameraMovementTest
- 목적: 카메라가 오직 내 플레이어를 추적하고 맵 경계에서 올바르게 클램핑되는지 검증
- 주요 케이스
  - 내 플레이어만 추적, 다른 플레이어 이동 무시, 내 플레이어 이동 시 추적, 맵 경계 클램프, 스냅샷에 내 ID가 없을 때 마지막 위치 유지

### ViewportTransformTest
- 목적: 뷰포트 월드/스크린 변환의 중심 좌표 정합성
- 주요 케이스
  - 월드 중심 → 스크린 중심 매핑, 줌 변경 후에도 중심 유지

---

## 3) comfps-server

### PlayerSyncServiceTest
- 목적: 캐릭터 위치 판단 및 스냅샷 생성의 정확성 검증
- 주요 케이스
  - testAddPlayerWithCharacter: 캐릭터가 있을 때 해당 위치로 스폰
  - testAddPlayerWithoutCharacter: 캐릭터 없을 때 맵 중앙 스폰
  - testRemovePlayer: 플레이어 제거 시 스냅샷에서 사라짐
  - testRemoveNonexistentPlayer: 존재하지 않는 플레이어 제거 시 예외 없음
  - testSnapshotIncludesHealthAndCooldowns: HP와 스킬 쿨다운 정보 포함
  - testTickUpdatesPosition: 틱 업데이트 시 플레이어 위치 이동
  - testBoundaryClamp: 맵 경계를 벗어나면 클램핑
  - testMultiplePlayers: 여러 플레이어 동시 관리
  - testEmptySnapshot: 플레이어 없을 때 빈 스냅샷 생성

### LobbyStateTest
- 목적: 로비 인원/준비 상태 관리
- 주요 케이스
  - join/leave에 따른 size, setReady/isReady, isEveryoneReady(최소 1명+전원 ready), resetReady, resetAll

### SessionRegistryTest
- 목적: 세션/플레이어/월드 관리
- 주요 케이스
  - 기본 닉네임/설정, setMapId로 월드 생성, 캐릭터 생성 시 팀 배정(RED/BLUE), 조준각 기본값 0f

### GameServerTest
- 목적: 서버 수명주기 및 투표/단계 트래킹
- 주요 케이스
  - 생성/시작/정지, 플레이어 입장/퇴장(없는 ID 퇴장 허용), 맵 투표/승자 계산, 투표 초기화, Phase 유효성

### DefaultServerRouterTest
- 목적: 서버 라우터의 패킷 라우팅과 훅 동작 검증(가짜 컨텍스트)
- 주요 케이스
  - CHAT 라우팅과 브로드캐스트, PING→PONG 응답, READY_TOGGLE 훅 호출, SET_SELECTION/MAP_VOTE 파싱, BYE/INPUT/ACTION 처리

---

## 커버리지 관점 요약
- 공통 수학/벡터/유틸리티/프로토콜: 기능별 정상/경계/예외 시나리오 포함
- 클라이언트: 네트워크 I/O 없이 리스너 경로 준비 상태를 단위 테스트로 검증
- 서버: 로비/세션/서버/라우팅의 핵심 경로를 페이크 환경으로 검증

## 에지 케이스/오류 모드
- 프로토콜: 잘못된 길이/버전/카운트/절단 페이로드 → 예외
- 수학: min>max, ±PI 경계, 외삽/동일범위/음수범위
- 스냅샷: null/empty, float 정밀 유지
- 서버: 존재하지 않는 ID 퇴장, 초기 상태에서의 호출들

## 실행 방법
- VS Code 터미널에서 루트로 이동 후 `mvn test` 실행
- 개별 모듈: `cd comfps-common && mvn test` 등으로 실행 가능

## 성공 기준(품질 게이트)
- Build: PASS (모든 모듈 컴파일 성공)
- Lint/Typecheck: PASS (테스트 컴파일 경고/에러 없음)
- Tests: PASS (전 테스트 케이스 성공)

## 참고
- 각 테스트 파일 상단/메서드에 한국어 주석으로 의도 설명 추가
- 네트워크·스레드 의존 경로는 페이크/단위 수준으로 격리하여 검증
