# 프로젝트 설계도 및 주차별 계획

본 문서는 Java 소켓 프로그래밍 기반 FPS 미니 프로젝트(클라이언트/서버)의 설계 개요와 주차별 진행 내역, 남은 작업 계획(스프린트)을 정리합니다. 현재 시점은 9주차 목요일입니다.

## 1–8주차 진행 내역

1주차 — 요구사항 정리/기술 스택 선정
- 목표: 자유 주제 네트워크 응용, Java/Swing + TCP 소켓, 단순 바이너리 프로토콜 확정
- 산출물: 초기 리포지토리 구조, 패키지 구분(client/server/common)

2주차 — 공통 라이브러리/프로토콜 스켈레톤
- 목표: `Protocol` 프레임 구조 정의(길이+opcode+payload), DataInput/Output 기반 직렬화 틀
- 산출물: `src/com/fpsgame/common/Protocol.java` 초안, 기본 Opcode 도출(CHAT, PING/PONG, WELCOME 등)

3주차 — 서버 부트스트랩/세션
- 목표: 서버 소켓 수립, 세션 관리, 기본 라우팅/브로드캐스트 구조
- 산출물: `server/*` 메인/루프/라우터 기본 골격, 더미 응답

4주차 — 클라이언트 NetClient + 최소 UI
- 목표: `NetClient`로 TCP 연결/프레임 송수신, `ClientMain`에 최소 Swing UI(채팅, Ping)
- 산출물: 연결/끊김/프레임 디스패치, 채팅 송수신

5주차 — 게임 상태(Phase)/카운트다운/라운드 결과
- 목표: Opcode 확장(PHASE_UPDATE/COUNTDOWN/ROUND_RESULT), HUD 표시
- 산출물: `HudPanel` 연동, 상태 업데이트 표시

6주차 — 로비/선택/맵 투표
- 목표: READY 토글, 팀/캐릭터 선택, 맵 투표 송수신
- 산출물: `CharacterSelectPanel`, 선택/투표 API, UI 반영

7주차 — 스냅샷/보간 모델 기초
- 목표: `PlayerSnapshotBuffer`로 위치/에임 스냅샷 수신 및 보간, v1/v2 스냅샷 메타(팀/캐릭)
- 산출물: 보간 샘플/확장 엔트리, v2 메타 병합

8주차 — UI 정리/주석/가독성 개선
- 목표: 화면 오버레이/알림, 주석 한글화, 문자열 일부 정리
- 산출물: `CenterMessageOverlay`, `CenterMessageOverlayPanel`, 다수 UI 파일 주석 정리

## 9–14주차 스프린트 계획(현재: 9주차 목요일)

9주차 — 네트워크 안정화/프로토콜 명세 고도화 [진행 중]
– 목표: 문서화 정리, 연결 안정성·복구 로직 점검, 개발용 체크리스트 마련
– 작업 항목(담당/산출/검증)
  - PROTOCOL.md 정리/배포 (문서 책임자) → PR 리뷰·합의 로그
  - README.md 정리(실행/데모 시나리오) (클라 담당) → 신규 인원 따라하기 테스트
  - NetClient 연결/끊김/재시도 시나리오 점검 (클라 담당) → 로컬 서버 다운/복구 테스트 통과
  - ServerSession 종료·정리 루틴 점검 (서버 담당) → 소켓 누수/스레드 종료 확인
  - 로컬 테스트 체크리스트 작성 (QA) → 저장소 scripts/ 혹은 docs/에 커밋
– 완료 조건: 문서 2종 갱신, 연결 수립/끊김/재시도 시 UI 피드백 정상

10주차 — 서버 라우팅/세션 수명 개선
– 목표: 세션·라우팅 신뢰도 향상
– 작업 항목
  - SessionRegistry 만료 정책/타임아웃 추가, 동시성 점검
  - Router 훅(접속/퇴장/이동) 명세화 및 로깅 일원화
  - 브로드캐스트/유니캐스트 경로 분리 및 단위 테스트(가능 시)
– 완료 조건: 세션 누수 0, 라우팅 로그로 시퀀스 추적 가능

11주차 — 게임 라운드 플로우 정합성
– 목표: 라운드 사이클 타이밍 정합성
– 작업 항목
  - PHASE/COUNTDOWN/ROUND_RESULT 전이 조건·타이밍 일치화(서버·클라)
  - READY 집계 정확도/중복 처리 방지
  - 라운드 종료 리셋(스코어/선택/타이머) 확인 및 누락 보완
– 완료 조건: 데모 시나리오 연속 10회 무오류 진행

12주차 — 성능/RTT/스냅샷 품질
– 목표: 체감 품질 개선
– 작업 항목
  - RTT 그래프/지표 시각화 개선(최댓값/평균/표준편차)
  - 보간 파라미터(interpDelayNs) A/B 튜닝 시트 작성
  - Snapshot v2 메타 활용 확대(HUD 팀/캐릭 정보 표기)
– 완료 조건: 테스트 기준 프레임 드랍/지연 체감 감소 보고

13주차 — 안정화/테스트/버그 수정
– 목표: 릴리스 안정화
– 작업 항목
  - 통합 시나리오(접속→라운드 N회→종료) 자동화 스크립트 준비(수동도 가능)
  - 예외/경계 조건(빈 페이로드, 알 수 없는 opcode) 방어 로직 보강
  - 메모리/스레드/소켓 누수 점검(도구/수동)
– 완료 조건: 크래시/멈춤 0, 알려진 치명적 버그 0

14주차 — 발표/배포 준비
– 목표: 제출/발표 품질 확보
– 작업 항목
  - 최종 README/슬라이드/데모 시나리오 정리
  - UI 라벨/문구 일관화, 키바인드 점검
  - 태그/릴리스 노트 작성
– 완료 조건: 리허설 1회 이상, 배포물 확정

## 아키텍처 개요
- Client(Java Swing) — `NetClient`로 TCP 통신/프레임 처리 → UI(HUD/선택/채팅) 갱신
- Server(Java) — 세션/라우팅/라운드 플로우 → 프레임 브로드캐스트
- Common — `Protocol`(opcode/직렬화), 스냅샷 모델(V1/V2), 유틸

## 리스크/대응
- 소켓 I/O 블로킹/예외: 타임아웃/정리 루틴 보강(9~10주차)
- 지연/보간 품질: interpDelayNs/샘플링 튜닝(12주차)
- 프로토콜/타이밍 일치: 서버/클라 정합성 테스트(11주차)
