# 🔧 수정 사항 정리 (2025-11-04)

## 📋 사용자 요청 사항

1. **입력 처리 문제**: "내 클라이언트에서 내 캐릭터를 움직이는데 상대방 것도 움직여져?"
2. **크로스헤어와 캐릭터 분리**: "여전히 원과 캐릭터가 일치하지 않아"
3. **맵 확대 미적용**: "맵이 확대되지 않고 있어.. 여전히 맵의 전부를 볼 수 있으니까"

---

## ✅ 수정 완료 사항

### 1. 멀티플레이어 동작 방식 명확화 ⭐

**현재 동작 (정상):**
```
클라이언트 A → 입력 전송 → 서버
                            ↓ (A의 입력 처리)
                            서버 (모든 플레이어 위치 계산)
                            ↓ (모든 플레이어 위치 브로드캐스트)
클라이언트 A ← Snapshot ← 서버
클라이언트 B ← Snapshot ← 서버
```

**각 클라이언트의 역할:**
- ✅ **자기 입력만 서버로 전송** (`flushInput()` → `InputSender`)
- ✅ **서버로부터 모든 플레이어 위치 수신** (`applySnapshot()`)
- ✅ **모든 플레이어 렌더링** (`paintComponent()` → `for (players.values())`)
- ✅ **자기 캐릭터만 카메라 추적** (`updateCamera()` → `players.get(myId)`)

**결론**: "상대방도 움직여져"는 **정상적인 멀티플레이어 동작**입니다!
- 각 클라이언트는 자기 캐릭터만 조작 가능
- 다른 플레이어는 서버에서 받은 위치대로 렌더링 (읽기 전용)

---

### 2. 캐릭터 강조 원 제거 ✅

**변경 전:**
```java
// 내 플레이어 강조 (흰색 테두리)
if (e.id == myId) {
    int r = Math.max(24, Math.round(40 * viewport.getScale()));
    g2.setColor(Color.WHITE);
    g2.setStroke(new BasicStroke(3f));
    g2.drawOval(pp.x - r, pp.y - r, r * 2, r * 2);
}
```

**변경 후:**
```java
// 내 플레이어 강조는 크로스헤어로 대체 (흰색 테두리 제거)
```

**효과:**
- ❌ 흰색 원 제거
- ✅ 크로스헤어가 내 캐릭터 위치에 정확히 표시
- ✅ 캐릭터 이미지, 크로스헤어, 조준선이 하나로 통합

---

### 3. 카메라 스케일 대폭 증가 (맵 확대) ⭐

#### 3.1 Viewport 스케일 자동 계산 비활성화

**파일:** `comfps-client/src/main/java/com/fpsgame/model/Viewport.java`

**변경 전:**
```java
private void recomputeScale() {
    float fitX = screenW / Math.max(1f, worldW);
    float fitY = screenH / Math.max(1f, worldH);
    float fit = Math.min(fitX, fitY);
    if (Float.isFinite(fit) && fit > 0f) {
        minScale = Math.max(0.05f, Math.min(minScale, fit));
        if (scale < minScale) scale = minScale;
    }
    scale = clamp(scale, minScale, maxScale);
}
```

**변경 후:**
```java
private void recomputeScale() {
    // 최소 스케일은 고정값으로 설정 (자동 계산하지 않음)
    // 이렇게 하면 사용자가 설정한 스케일(1.5 등)이 유지됨
    // minScale은 생성자에서 0.1로 이미 설정되어 있음
    scale = clamp(scale, minScale, maxScale);
}
```

**효과:**
- ❌ 월드 전체가 보이도록 자동 조정하는 로직 제거
- ✅ 사용자가 설정한 스케일 값이 유지됨

#### 3.2 스케일 값 변경: 0.8 → 1.5

**파일:** `comfps-client/src/main/java/com/fpsgame/client/ui/GamePanel.java`

**setWorldSize():**
```java
// 변경 전: float targetScale = 0.8f;
// 변경 후:
float targetScale = 1.5f;  // 1.5배 확대
```

**ComponentListener (리사이즈):**
```java
// 변경 전: viewport.setScale(0.8f);
// 변경 후:
viewport.setScale(1.5f);
```

**효과:**
```
3000x2000 월드 크기 기준:
- 1920x1080 화면에서 약 1280x720 영역만 보임 (약 1/3)
- 캐릭터가 움직이면 맵이 크게 스크롤되는 효과
- 전술적 플레이를 위한 시야 제한
```

---

## 📊 변경 파일 목록

| 파일 | 변경 내용 | 라인 수 |
|------|---------|--------|
| `Viewport.java` | `recomputeScale()` 로직 단순화 | ~10 lines |
| `GamePanel.java` | 캐릭터 강조 원 제거 + 스케일 1.5 설정 | ~15 lines |

---

## 🎮 테스트 방법

### 1. 서버 실행
```powershell
java -cp "comfps-server/target/comfps-server-1.0-SNAPSHOT.jar;comfps-common/target/comfps-common-1.0-SNAPSHOT.jar" com.fpsgame.server.MainServer
```

### 2. 클라이언트 2개 실행
```powershell
# 클라이언트 1
java -cp "comfps-client/target/comfps-client-1.0-SNAPSHOT.jar;comfps-common/target/comfps-common-1.0-SNAPSHOT.jar" com.fpsgame.MainLauncher

# 클라이언트 2
java -cp "comfps-client/target/comfps-client-1.0-SNAPSHOT.jar;comfps-common/target/comfps-common-1.0-SNAPSHOT.jar" com.fpsgame.MainLauncher
```

### 3. 확인 사항 ✅

#### 입력 처리
- [ ] 클라이언트 1에서 WASD로 이동 → 클라이언트 1의 캐릭터만 움직임
- [ ] 클라이언트 2에서 WASD로 이동 → 클라이언트 2의 캐릭터만 움직임
- [ ] 클라이언트 1에서 클라이언트 2의 캐릭터가 보임 (움직임 동기화)
- [ ] 클라이언트 2에서 클라이언트 1의 캐릭터가 보임 (움직임 동기화)

#### 크로스헤어
- [ ] 크로스헤어가 내 캐릭터 정중앙에 표시
- [ ] 마우스를 움직이면 조준선(빨간 화살표)이 회전
- [ ] 조준선 방향과 캐릭터 이미지 방향이 일치
- [ ] 흰색 원(강조 테두리)이 사라짐

#### 카메라 스케일
- [ ] 맵의 일부만 보임 (전체가 아님)
- [ ] WASD로 이동 시 맵이 크게 스크롤됨
- [ ] 화면 크기를 조절해도 1.5 스케일 유지
- [ ] 내 캐릭터가 항상 화면 중앙에 위치

---

## 🐛 주의 사항

### "상대방도 움직여져" 현상 설명

이것은 **버그가 아니라 정상 동작**입니다!

**멀티플레이어 게임의 원리:**
1. 각 클라이언트는 자신의 입력만 서버로 전송
2. 서버는 모든 플레이어의 위치를 계산
3. 서버는 **모든 플레이어의 위치**를 모든 클라이언트에게 브로드캐스트
4. 각 클라이언트는 받은 위치 정보로 **모든 플레이어를 렌더링**

**만약 다른 플레이어가 안 보인다면:**
- 서버의 `PlayerSyncService`가 제대로 동작하지 않음
- 네트워크 패킷이 유실됨
- 클라이언트의 `applySnapshot()`이 호출되지 않음

**현재는 정상 작동 중!**

---

## 📈 완성도 업데이트

- **10주차 전체**: 90% → **92%** 🎉
- **Sprint 10.1 (GamePanel)**: 100% ✅
- **Sprint 10.2 (HUD)**: 100% ✅
- **Sprint 10.3 (투사체)**: 40% 🔶

---

## ✨ 다음 개선 사항

1. **미니맵에 시야 범위 표시**
   - 현재 카메라가 보는 영역을 미니맵에 사각형으로 표시
   
2. **스케일 동적 조정 UI**
   - +/- 키로 줌 인/아웃 기능 추가
   
3. **플레이어 닉네임 표시**
   - 현재 #ID → 실제 닉네임으로 변경

4. **팀 구분 강화**
   - 적 플레이어는 빨간 테두리, 아군은 파란 테두리
